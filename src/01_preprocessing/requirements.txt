# WES CNV Pipeline Python Requirements
# Install with: pip install -r requirements.txt

# Core CNV analysis
cnvkit>=0.9.9

# Data manipulation
pandas>=1.3.0
numpy>=1.21.0

# File format support
openpyxl>=3.0.0

# Visualization (optional but recommended)
matplotlib>=3.3.0
scipy>=1.7.0

# BAM file handling (optional, CNVkit dependency)
pysam>=0.16.0

# Note: For CBS segmentation method, you also need R with DNAcopy:
# In R console:
#   if (!require("BiocManager", quietly = TRUE))
#       install.packages("BiocManager")
#   BiocManager::install("DNAcopy")